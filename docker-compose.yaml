version: "3.9"

services:

  database:
    image: postgres:latest
    container_name: postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: cards-shared
    ports:
      - 5432:5432